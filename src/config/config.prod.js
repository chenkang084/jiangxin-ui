const config = {
  app: "jx",
  uri: {
    editor: {
      // api: "https://reg.monkeystar.cn/"
      // api: "http://localhost:9002/"
      api: "/"
    },
    iframeUri: "/"
  }
};

export default config;
